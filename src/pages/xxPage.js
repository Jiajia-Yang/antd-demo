import React from 'react';
import { connect } from 'react-redux';
import { actions } from '@actions';

// @connect((state) => {
//   return { ...state };
// }, { ...actions })
class XXPage extends React.Component {
  render() {
    return (
      <div>
        xxxpage
      </div>
    );
  }
}
export default XXPage;
